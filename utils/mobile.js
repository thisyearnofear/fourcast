/**
 * Mobile hide utility — show only on breakpoints that match user needs.
 * 
 * Pattern: `hidden sm:block` hides on screens < 640px, visible on sm+.
 * Use these class strings directly in components for mobile content trimming.
 */
export const MOBILE_HIDE = 'hidden sm:block';
export const MOBILE_HIDE_LG = 'hidden lg:block';