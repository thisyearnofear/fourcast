# Keep deploy checkout lean. Git deploys still clone tracked files, but CLI
# uploads and Next NFT tracing should not treat these as app payload.
# Do NOT ignore onchain/ — WalletLayer imports onchain/config at build time.
# Do NOT ignore scripts/ — package.json prepare runs chmod on scripts/*.sh
lib/openzeppelin-contracts
videos
media
canton
contracts
telegraph-miner
tests
docs
deploy
.git
*.md
!README.md
*.db
*.db-shm
*.db-wal
*.mp4
*.mov
*.pdf
*.dar
